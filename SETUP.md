# セットアップ・使い方

麻雀トラッカー（MaaNanoJa）の導入方法と、当日の使い方をまとめたもの。アプリの紹介は [README.md](./README.md) を参照。

## 動かす（開発）

```bash
npm install
npm run dev      # 開発サーバー
```

```bash
npm run build    # 本番ビルド（型チェック込み, dist/）
npm run preview  # ビルド結果をプレビュー
npm test         # スコア計算のテスト（既存スプレッドシートの9試合と一致するか検証）
npm run check    # typecheck + lint + format:check + test（コミット前に緑にする）
```

ビルドは相対パス出力（`base: './'`）なので、`dist/` をそのまま開いても・任意の静的ホスティングに置いても動く。

## 使い方（アプリ内)

1. 「設定」タブでメンバー（最大4人ぶん名前を登録）とルール（持ち点・返し点・ウマ）を確認する。既定値は namimori 氏のスプレッドシートに合わせてある。
2. 「記録」タブで席を選んで半荘を開始する。
   - 「局ログで記録」… 和了（ロン / ツモ）・流局・途中流局を1局ずつ記録。翻符を選ぶと点数移動・本場・供託リーチ棒・親の連荘まで自動計算される。
   - 「最終点だけ入力」… 従来どおり終局の持ち点だけ入力する簡易モード。
3. 半荘を終えると順位・スコアが自動算出される。「履歴」タブで過去の半荘と局ログを見返せる。
4. 単独利用のデータはブラウザの localStorage に保存される（外部送信なし）。バックアップ・機種変更は「設定」タブの JSON 書き出し／読み込みを使う。

## Cloudflareで公開する（ルーム共有）

Cloudflare Workers + D1へ公開すると、同じルームコードを知る4人が別WiFiから同じ対局を操作できる。ログインはなく、コードを知る人はそのルームを閲覧・編集できるため、コードは必要な人だけに共有する。

### ローカルで確認

```bash
npm run cf:db:local
npm run cf:dev
```

Worker URLを開き、「新しいルームを作る」。PCは成績タブからモニター表示を開き、スマホにはルームURLを共有する。進行中の局はPCに現在点として表示され、半荘を保存すると履歴が同じルームへ蓄積される。

### 本番公開

1. `npx wrangler login` でCloudflareにログインする。
2. `npx wrangler d1 create maananaja` を一度だけ実行し、表示された `database_id` を `wrangler.jsonc` の `d1_databases` に設定する。
3. `npm run cf:db:remote` でD1 migrationを適用する。
4. `npm run cf:deploy` でWorkersへ公開する。

`workers_dev` が有効なので、初期状態では `*.workers.dev` のURLでアクセスできる。カスタムドメインは後から追加できる。Cloudflareアカウントへのログイン、D1作成、remote migration、deployは外部状態を変更するため、実行前に対象アカウントとデータベースを確認する。

既存ブラウザのlocalStorage履歴は、ルーム作成時に自動アップロードしない。共有ルームで記録を始めた後の対局はD1へ保存される。既存履歴の移行が必要な場合は、JSONバックアップを別に保管したうえで移行機能を追加する。

## みんなで同時に使う（LAN同期・家麻雀の当日）

同じWiFiにつないだ **PCとスマホから、同じデータを見て・入力する** ための任意モード。PCモニターに全員ぶんを映しつつ、記録はスマホ・PCどちらからでもできる。

```bash
npm run host   # ビルド＋LAN同期サーバ起動（当日はこれ1つ）
```

起動ログに出るURL（`http://<PCのローカルIP>:5180`）を、PCでもスマホでも開くだけ。データはPC上の `server/data/db.json` に保存され、**外部には一切送らない**（同じWiFi内だけで完結）。サーバを起動していないときは、これまでどおり各端末の localStorage だけで動く。

進行中の半荘（`db.draft`）もDBごと同期するので、誰かが席を選んで半荘を始めると全端末がその入力画面になる。観戦専用の読み取りUIはなく、全員が同じ入力画面を共有して誰でも局を追加できる。設計の詳細・割り切りは [CLAUDE.md](./CLAUDE.md) の「LAN同期モード」を参照。

**WiFiが無い/不安定なとき**は、Androidスマホの「USBテザリング」をONにしてPCとUSB-Cケーブルで直結しても同じように使える。ケーブルを繋ぐとPC側に新しいIPアドレスが増えるので、起動ログに追加で表示されるそのURLをスマホのブラウザで開けばよい（サーバ側の設定変更は不要。iPhoneはUSB接続だけではPC向けにネットワークを開放しないため、この方法は今のところAndroid想定）。

**モニター表示（`?board=1`）:** 「成績」タブの「🖥 モニターで表示」リンクから、順位・名前・合計スコアだけを大きく表示する全画面ボードを開ける。誰かの端末をテレビ/モニターに繋いで新しいタブで開く用途。

### LAN同期が繋がらないとき

家庭用ルーター/メッシュ機器の「APアイソレーション（クライアント分離）」で、同じWiFi内でも端末間通信がブロックされることがある。USBテザリングでも解決しない場合の代替案として、**Chromeリモートデスクトップ**でスマホからPCの画面を遠隔操作する運用がある。

- PC側: https://remotedesktop.google.com/access で「リモートアクセスを設定」→ PIN設定 → オンラインにする。
- スマホ側: 「Chrome リモートデスクトップ」アプリを同じGoogleアカウントでログインし、PCを選んでPIN入力。
- あとはPC上でいつもどおりアプリを使うだけ（`npm run dev` でも `npm run host` でも可）。ただし同時操作者は1人（1接続）が前提。

詳細は [CLAUDE.md](./CLAUDE.md) の追記を参照。

## スコアのルール（初期値）

- 25,000点持ち / 30,000点返し
- ウマ 10-30（1位 +30 / 2位 +10 / 3位 -10 / 4位 -30）
- オカは（返し点 − 持ち点）× 人数 を1位に自動加算
- 同点は上家（起家に近い方）優先

これらは設定画面で変更できる。計算の仕様は [SPEC.md](./SPEC.md) を参照。

## 開発ルール

- **[CLAUDE.md](./CLAUDE.md)** — 設計ガイド（どこに・何を・どう書くか、守るべきルール）。作業前にまず読む。
- **[SPEC.md](./SPEC.md)** — 振る舞いの仕様（スコア計算・データモデル・不変条件）。

## テスト

`src/lib/scoring.test.ts` に、既存スプレッドシートの過去9試合の最終持ち点 → スコアが**完全一致**することを検証するテストがある。和了点の早見表・点数移動（ロン/ツモ/流局）も併せて検証。「実行できる仕様」として維持する。

`src/lib/remote.test.ts` は、LAN同期で「サーバの変更を取り込むべきか」の判定（純粋関数）を検証する。
