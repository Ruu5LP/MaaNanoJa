# 麻雀トラッカー

月イチの麻雀（4人打ち）のスコアを記録して、**放銃率・和了率・立直率**などのデータまで取るためのローカルツール。

いつも使っている [namimori 氏の麻雀集計スプレッドシート](https://hirokuasaku-live.blogspot.com/2022/08/mahjong-spreadsheet-new.html) のスコア計算をそのまま再現したうえで、**局ごとの記録（和了 / 放銃 / 流局 / 立直）**を足して、そこから統計を出せるようにしたもの。過去9試合のデータを初期値として入れてあるので、開いた瞬間から今までの成績が見える。

## できること

- **記録**
  - 「局ログで記録」… 和了（ロン / ツモ）・流局・途中流局を1局ずつ記録。翻符を入れると点数移動・本場・供託リーチ棒・親の連荘/流れまで**自動計算**され、持ち点がリアルタイムに動く。
  - 「最終点だけ入力」… 従来どおり終局の持ち点だけ入力。
  - 終局で順位 → ウマ・オカ込みのスコアを自動算出。
- **成績**
  - 合計スコア / 平均スコア / 平均順位 / 着順分布 / トップ率 / 連対率 / ラス率 / トビ率 / 平均素点
  - 局ログがあれば：**和了率 / 放銃率 / 立直率 / ツモ率 / 平均和了点 / 平均放銃点 / テンパイ率**
- **履歴**：半荘ごとの結果と局ログを閲覧・削除。
- **設定**：メンバー・ルール（持ち点 / 返し点 / ウマ）の変更、JSONでの書き出し／読み込み。

## スコアのルール（初期値）

- 25,000点持ち / 30,000点返し
- ウマ 10-30（1位 +30 / 2位 +10 / 3位 -10 / 4位 -30）
- オカは（返し点 − 持ち点）× 人数 を1位に自動加算
- 同点は上家（起家に近い方）優先

これらは設定画面で変更できる。

## データの保存先

データはブラウザの localStorage にのみ保存される（サーバー送信なし）。別端末に移したりバックアップするときは、設定画面の「JSONで書き出し／読み込み」を使う。

## みんなで同時に使う（LAN同期・家麻雀の当日）

同じWiFiにつないだ**PCとスマホから、同じデータを見て・入力する**ための任意モード。PCモニターに全員ぶんを映しつつ、記録はスマホ・PCどちらからでもできる。

```bash
npm run host   # ビルド＋LAN同期サーバ起動（当日はこれ1つ）
```

起動ログに出るURL（`http://<PCのローカルIP>:5180`）を、PCでもスマホでも開くだけ。データはPC上の `server/data/db.json` に保存され、**外部には一切送らない**（同じWiFi内だけで完結）。サーバを起動していないときは、これまでどおり各端末の localStorage だけで動く。詳しい設計と割り切りは [CLAUDE.md](./CLAUDE.md) の「LAN同期モード」。

## 使い方（開発）

```bash
npm install
npm run dev      # 開発サーバー
npm run build    # 本番ビルド（型チェック込み, dist/）
npm run preview  # ビルド結果をプレビュー
npm test         # スコア計算のテスト（既存スプレッドシートの9試合と一致するか検証）
npm run check    # typecheck + lint + format:check + test（コミット前に緑にする）
```

ビルドは相対パス出力（`base: './'`）なので、`dist/` をそのまま開いても・任意の静的ホスティングに置いても動く。

## 技術

- React 18 + Vite + TypeScript（strict）
- テスト: Vitest / Lint: ESLint / 整形: Prettier（CIでゲート化）
- 外部サービス・クラウドなし。基本はブラウザ内で完結（任意でLAN内だけの同期サーバを起動できる）。

## 開発ルール・仕様

- **[CLAUDE.md](./CLAUDE.md)** — 設計ガイド（どこに・何を・どう書くか、守るべきルール）。作業前にまず読む。
- **[SPEC.md](./SPEC.md)** — 振る舞いの仕様（スコア計算・データモデル・不変条件）。

## テスト

`src/lib/scoring.test.ts` に、既存スプレッドシートの過去9試合の最終持ち点 → スコアが**完全一致**することを検証するテストがある。和了点の早見表・点数移動（ロン/ツモ/流局）も併せて検証。「実行できる仕様」として維持する。
